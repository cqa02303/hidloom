# HIDloom operator environment example.
# Copy this file to .env, keep that file untracked, and replace only local values.
HIDLOOM_ACTIVE_DEVICE=XX
HIDLOOM_SSH_TARGET=keyboard.example
HIDLOOM_HTTP_STATUS_URL=https://keyboard.example/api/status
HIDLOOM_DEVICE_XX_SSH_TARGET=keyboard.example
